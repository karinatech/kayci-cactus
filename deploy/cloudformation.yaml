AWSTemplateFormatVersion: '2010-09-09'
Description: 'Kayci Cactus — Massage booking website (Amplify + Lambda + API Gateway + DynamoDB)'

Parameters:
  GitHubOwner:
    Type: String
    Default: karinatech
  GitHubRepo:
    Type: String
    Default: kayci-cactus
  GitHubBranch:
    Type: String
    Default: main
  GitHubToken:
    Type: String
    NoEcho: true
  SpecialistEmail:
    Type: String
    Default: specialist@example.com
  DeployBucketName:
    Type: String
    Default: kayci-cactus-deploy-061051222996
    Description: Pre-created S3 bucket containing lambda.zip

Resources:
  # ── DynamoDB Table ──
  KayciDynamoTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: kayci-cactus-data
      AttributeDefinitions:
        - AttributeName: id
          AttributeType: S
      KeySchema:
        - AttributeName: id
          KeyType: HASH
      BillingMode: PAY_PER_REQUEST

  # ── IAM Role for Lambda ──
  KayciLambdaRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: kayci-cactus-lambda-role
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: KayciDynamoDBAccess
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - dynamodb:PutItem
                  - dynamodb:GetItem
                  - dynamodb:UpdateItem
                  - dynamodb:DeleteItem
                  - dynamodb:Scan
                  - dynamodb:Query
                Resource: !GetAtt KayciDynamoTable.Arn
        - PolicyName: KayciSESAccess
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - ses:SendEmail
                  - ses:SendRawEmail
                Resource: '*'

  # ── Lambda Function ──
  KayciLambdaFunction:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: kayci-cactus-api
      Runtime: nodejs20.x
      Handler: index.handler
      Role: !GetAtt KayciLambdaRole.Arn
      Timeout: 30
      MemorySize: 256
      Environment:
        Variables:
          DDB_TABLE: !Ref KayciDynamoTable
          SPECIALIST_EMAIL: !Ref SpecialistEmail
          SPECIALIST_NAME: 'Kayci Cactus'
          ADMIN_TOKEN: 'saguaro2024'
      Code:
        S3Bucket: !Ref DeployBucketName
        S3Key: lambda.zip

  # ── API Gateway ──
  KayciApi:
    Type: AWS::ApiGateway::RestApi
    Properties:
      Name: kayci-cactus-api
      Description: API for Kayci Cactus massage booking
      EndpointConfiguration:
        Types:
          - REGIONAL

  ApiResource:
    Type: AWS::ApiGateway::Resource
    Properties:
      ParentId: !GetAtt KayciApi.RootResourceId
      PathPart: 'api'

  ApiProxyResource:
    Type: AWS::ApiGateway::Resource
    Properties:
      ParentId: !Ref ApiResource
      PathPart: '{proxy+}'

  ApiProxyMethod:
    Type: AWS::ApiGateway::Method
    Properties:
      ResourceId: !Ref ApiProxyResource
      RestApiId: !Ref KayciApi
      HttpMethod: ANY
      AuthorizationType: NONE
      Integration:
        Type: AWS_PROXY
        IntegrationHttpMethod: POST
        Uri: !Sub arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/${KayciLambdaFunction.Arn}/invocations

  ApiRootMethod:
    Type: AWS::ApiGateway::Method
    Properties:
      ResourceId: !Ref ApiResource
      RestApiId: !Ref KayciApi
      HttpMethod: ANY
      AuthorizationType: NONE
      Integration:
        Type: AWS_PROXY
        IntegrationHttpMethod: POST
        Uri: !Sub arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/${KayciLambdaFunction.Arn}/invocations

  KayciLambdaApiPermission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref KayciLambdaFunction
      Action: lambda:InvokeFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${KayciApi}/*/*/*

  KayciApiDeployment:
    Type: AWS::ApiGateway::Deployment
    DependsOn:
      - ApiProxyMethod
      - ApiRootMethod
    Properties:
      RestApiId: !Ref KayciApi

  KayciApiStage:
    Type: AWS::ApiGateway::Stage
    Properties:
      RestApiId: !Ref KayciApi
      DeploymentId: !Ref KayciApiDeployment
      StageName: prod

  # ── IAM Role for Amplify ──
  KayciAmplifyRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: kayci-cactus-amplify-role
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: amplify.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/PowerUserAccess

  # ── Amplify App ──
  KayciAmplifyApp:
    Type: AWS::Amplify::App
    Properties:
      Name: kayci-cactus
      Description: Kayci Cactus massage therapy website
      Repository: !Sub https://github.com/${GitHubOwner}/${GitHubRepo}
      AccessToken: !Ref GitHubToken
      Platform: WEB
      BuildSpec: |
        version: 1
        frontend:
          phases:
            build:
              commands:
                - echo "window.API_BASE = '$API_URL';" > public/config.js
                - cp -r public/* .
          artifacts:
            baseDirectory: /
            files:
              - '**/*'
          cache:
            paths: []
      CustomRules:
        - Source: '/<.*>'
          Target: '/index.html'
          Status: '200'
      EnvironmentVariables:
        - VariableName: API_URL
          VariableValue: !Sub https://${KayciApi}.execute-api.${AWS::Region}.amazonaws.com/prod
      IAMServiceRole: !GetAtt KayciAmplifyRole.Arn
      EnableBranchAutoBuild: true

  # Amplify branch — uses !Ref (returns AppId), NOT !GetAtt
  KayciAmplifyBranch:
    Type: AWS::Amplify::Branch
    Properties:
      AppId: !Ref KayciAmplifyApp
      BranchName: !Ref GitHubBranch
      DisplayName: Production
      EnableAutoBuild: true
      Stage: PRODUCTION

Outputs:
  WebsiteURL:
    Description: Amplify website URL
    Value: !Sub https://${GitHubBranch}.${KayciAmplifyApp}.amplifyapp.com
  ApiURL:
    Description: API Gateway URL
    Value: !Sub https://${KayciApi}.execute-api.${AWS::Region}.amazonaws.com/prod
  DynamoDBTable:
    Description: DynamoDB table name
    Value: !Ref KayciDynamoTable
  LambdaFunction:
    Description: Lambda function name
    Value: !Ref KayciLambdaFunction
  AmplifyAppId:
    Description: Amplify App ID
    Value: !Ref KayciAmplifyApp